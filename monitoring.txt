#Architecture: uname -a
#CPU physical : cat /proc/cpuinfo | grep "physical id" | sort | uniq | wc -l	
#vCPU : cat /proc/cpuinfo | grep processor | wc -l
#Memory Usage : neofetch memory --memory_percent on | tr -d 'memory: '
#Disk Usage :
# CPU load :
# Last boot :
# LVM use :
# Connexions TCP :
# User log :
# Network : 
# Sudo :
